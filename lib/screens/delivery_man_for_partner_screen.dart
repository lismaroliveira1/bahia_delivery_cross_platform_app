import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class DeliveryManForPartnerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: DeliveryManForPartnerTab(),
    );
  }
}
