import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class BeAPartnerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BeAPartnerTab(),
    );
  }
}
