import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class ChatDeliveryManScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ChatDeliveryManTab(),
    );
  }
}
