import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class UserChatListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: UserChatListTab(),
    );
  }
}
