import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class InsertNewSectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: InsertNewSectionTab(),
    );
  }
}
