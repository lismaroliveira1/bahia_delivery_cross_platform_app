import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class SetupDeliveryManScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SetupDeliveryManTab(),
    );
  }
}
