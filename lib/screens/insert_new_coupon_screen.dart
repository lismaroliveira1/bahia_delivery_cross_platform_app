import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class InsertNewCouponScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: InsertNewCouponTab(),
    );
  }
}
