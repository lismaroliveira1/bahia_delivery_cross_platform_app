import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class SetupPartnerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SetupPartnerTab(),
    );
  }
}
