import 'package:flutter/material.dart';

import '../tabs/tabs.dart';

class SetupUserScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SetupUserTab(),
    );
  }
}
